base = float(input('Ingrese la base del triangulo: '))
altura = float(input('Ingrese la altura del triangulo: '))

superficie = (base * altura)/2

print('la superficie del triangulo es: {:.2f}'.format(superficie))
